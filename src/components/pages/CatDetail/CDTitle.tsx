const CDTitle = ({ title }: { title: string }) => {
  return (
    <h1 className="text-2xl font-bold text-primary-content bg-primary text-center">
      {title}
    </h1>
  );
};
export default CDTitle;
